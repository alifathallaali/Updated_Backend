# Executive Narrative

Every run can expose grounded cards: What Happened, Why It Matters, Evidence, Watchout and Next Analysis.
The narrative is deterministic and uses existing run outputs only. It explicitly does not add causal inference.
