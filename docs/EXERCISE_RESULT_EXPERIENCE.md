# Exercise Result Experience

Each engine result now includes deterministic `next_exercises` metadata in addition to presentation metadata, KPIs, charts, evidence, warnings and provenance.
Persisted execution routes also return the saved ProductRun `id`, allowing the UI to hand a completed run directly to the Report Builder.
