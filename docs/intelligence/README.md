# Backend Placement
Runtime implementation belongs in the existing `Updated-Backend/app/`.
Database changes belong in `Updated-Backend/migrations/`.
Backend tests belong in `Updated-Backend/tests/`.
This folder is guidance only. Inspect the existing backend before creating runtime modules.
Reuse existing `src/` intelligence engines where appropriate.
